#![allow(warnings)]

fn main() {

}
//^ Lifetimes
//- basics of scope & lifetimes
//- borrowed vs owned data wrt lifetimes
//- lifetimes annotation in fn defination
//- how &str (string literal vs string slice) differs?
//- struct & struct impl with lifetimes
//- Lifetime Elision rules

// ~ different struct types
// ~ struct impl (fn, method, associated fn)
// ~ enum, Option
// ~ pattern matching for enum
// ~ if let




// ~ vaiables (immutable & mutable), constants
// ~ scalar types (integer, foat, boolen, char)
// ~ compund types (tuple, array)
// ~ fn, fn parameters, return types
// ~ if-else, statement vs exprssion
// ~ loop, while, for, range

// Ep5 Rust Programming Tutorial for Beginners
// ~ stack & heap
// ~ string literals & String
// ~ copy scalar types vs move data in heap
// ~ fn & ownership
// ~ pass references, mutable & immutable references
// ~ dangling references
// ~ slice types

// Github: https://github.com/proful/youtube-snippets
// Twitter: https://twitter.com/profulsadangi

// packages => build, test & share crates
// crates => tree of modules that produces exe or lib
// modules => control org, scope & privacy of paths
// paths => way of naming struct, fn, module